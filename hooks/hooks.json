{
  "description": "Rewrites your prompt before you send it, using the settings in ~/.claude/boost.json, and hands the rewrite back to your input box in a cleared session. Never changes how the answer is written.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/prompt_boost.py\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
