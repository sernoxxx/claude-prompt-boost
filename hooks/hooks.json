{
  "description": "Sharpens the user's request before the model acts on it, using the settings in ~/.claude/boost.json. Never changes how the answer is written.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/prompt_boost.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
